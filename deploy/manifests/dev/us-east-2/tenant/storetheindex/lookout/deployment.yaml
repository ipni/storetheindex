apiVersion: apps/v1
kind: Deployment
metadata:
  name: lookout
spec:
  template:
    spec:
      containers:
        - name: lookout
          resources:
            limits:
              cpu: "0.3"
              memory: 512Mi
            requests:
              cpu: "0.3"
              memory: 512Mi
