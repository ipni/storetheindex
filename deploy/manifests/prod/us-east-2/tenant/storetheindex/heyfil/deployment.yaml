apiVersion: apps/v1
kind: Deployment
metadata:
  name: heyfil
spec:
  template:
    spec:
      containers:
        - name: heyfil
          resources:
            limits:
              cpu: "1"
              memory: 2Gi
            requests:
              cpu: "1"
              memory: 2Gi
            
