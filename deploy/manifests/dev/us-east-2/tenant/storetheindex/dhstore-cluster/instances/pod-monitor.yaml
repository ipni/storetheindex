apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dhstore-single
  labels:
    app: dhstore-single
spec:
  selector:
    matchLabels:
      app: dhstore-single
  namespaceSelector:
    matchNames:
      - storetheindex
  podMetricsEndpoints:
    - path: /metrics
      port: metrics
