apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dhfind
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt"
spec:
  tls:
    - hosts:
        - dhfind-cora.dev.cid.contact
      secretName: dhfind-cora-ingress-tls
  rules:
    - host: dhfind-cora.dev.cid.contact
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dhfind-cora
                port:
                  number: 80
