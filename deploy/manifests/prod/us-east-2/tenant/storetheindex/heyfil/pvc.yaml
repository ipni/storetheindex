apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: heyfil
  name: heyfil
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
  storageClassName: gp3