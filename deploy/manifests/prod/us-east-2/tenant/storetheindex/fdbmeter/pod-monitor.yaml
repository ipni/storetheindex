apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: fdbmeter
  labels:
    app: fdbmeter
spec:
  selector:
    matchLabels:
      app: fdbmeter
  namespaceSelector:
    matchNames:
      - storetheindex
  podMetricsEndpoints:
    - path: /metrics
      port: http
