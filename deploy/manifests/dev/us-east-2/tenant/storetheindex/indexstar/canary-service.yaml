kind: Service
apiVersion: v1
metadata:
  name: indexstar-canary
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: indexstar-canary
  type: ClusterIP
  clusterIP: None
