apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dhfind
spec:
  selector:
    matchLabels:
      app: dhfind-stateless
  namespaceSelector:
    matchNames:
      - storetheindex
  podMetricsEndpoints:
    - path: /metrics
      port: metrics
