apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: heyfil
  name: heyfil
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: gp3