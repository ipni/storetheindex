package client

import "github.com/libp2p/go-libp2p-core/protocol"

const (
	// ProviderProtocolID is the libp2p protocol ID used by the provider API.
	ProviderProtocolID protocol.ID = "/indexer/provider/0.0.1"
)
