apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dhfind
  labels:
    app: dhfind-cora
spec:
  selector:
    matchLabels:
      app: dhfind-cora
  namespaceSelector:
    matchNames:
      - storetheindex
  podMetricsEndpoints:
    - path: /metrics
      port: metrics
