apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
spec:
  resources:
    requests:
      storage: 10Ti
  dataSource:
    name: kepa-dido-021122
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  storageClassName: io2

