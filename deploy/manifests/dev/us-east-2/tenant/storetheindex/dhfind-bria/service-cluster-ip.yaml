apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dhfind
  labels:
    app: dhfind-bria
spec:
  selector:
    matchLabels:
      app: dhfind-bria
  namespaceSelector:
    matchNames:
      - storetheindex
  podMetricsEndpoints:
    - path: /metrics
      port: metrics
